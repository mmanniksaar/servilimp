/* document.addEventListener('DOMContentLoaded', function() {
    var navbarIcon = document.getElementById('navbarIcon');
    var navbarLinks = document.getElementById('navbarLinks');

    function toggleNavbar() {
        navbarLinks.classList.toggle('show');
        console.log('Toggle Navbar called');

    }

    // Lisatud tingimus, et kontrollida, kas element on olemas enne kuulaja lisamist
    if (navbarIcon && navbarLinks) {
        navbarIcon.addEventListener('click', toggleNavbar);
        console.log('Navbar Icon click listener added');
    }
    else {
        console.error('Navbar Icon or Links not found');
    }
}); */
/* function toggleNavbar() {
    var x = document.getElementById("navbarLinks");
    if (x.style.display === "block") {
      x.style.display = "none";
    } else {
      x.style.display = "block";
    }
  } */